// lib/src/core/constants/app_constants.dart

class AppConstants {
  static const String appName = 'Workshop Finder'; // Example App Name
  static const double defaultPadding = 16.0;
  static const double defaultRadius = 8.0;

  // API Endpoints (example)
  // static const String baseUrl = 'https://api.example.com';
  // static const String workshopsEndpoint = '/workshops';

  // Asset paths
  // static const String logoPath = 'assets/logo.png';
}

// You can also define enums here if they are core and widely used
enum VehicleType { car, motorcycle, bicycle, truck }
